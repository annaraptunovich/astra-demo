# Astra Demo

Simple Vite app for the Astra design system components and existing build-screen surfaces.

## Running The App

Install dependencies:

```sh
npm i
```

Start the development server:

```sh
npm run dev
```

Run verification:

```sh
npm run type-check
npm test
npm run build
```
